drivers:
  waits:
    implicit: 2
    downloadTimeout: 5
  chrome:
    args:
      - --headless=new
  events:
    afterGetAttribute:
      level: TRACE

data:
  folder: data
  fqdn: io.github.giulong.spectrum.verify_browsers.data.Data
